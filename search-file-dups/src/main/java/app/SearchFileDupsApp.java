package app;

import console.ConsoleInterfaceHandler;

// TODO Merge this project to file-tools module or project (if it was already extracted from this repository)
public class SearchFileDupsApp {

    public static void main(String[] args) throws Exception {
        ConsoleInterfaceHandler handler = new ConsoleInterfaceHandler();
        handler.start();
    }
}
